import { createContext } from 'react';

export const OneContext = createContext(false);
export const TwoContext = createContext(false);
export const ThreeContext = createContext(false);
export const FourContext = createContext(false);
